# Versions alignées sur l'environnement déjà validé dans le TP Chapitre 2
# (td/tp_chapitre_2/tp_chapitre2_pipeline/requirements.txt) pour limiter les
# risques d'incompatibilité dlt / dbt-duckdb / dagster.

# Versioning des données (dataset qui grandit au fil des ingestions
# bancaires / simulations de dérive — voir docs/)
dvc==3.67.1

# Ingestion
# 1.27.0 a été yanked par PyPI (bug de perte de données : le merge
# incrémental tronque la table de destination) ; correction en 1.27.2
dlt==1.27.2

# Stockage analytique
duckdb==1.5.3

# Transformation
dbt-core==1.11.11
dbt-duckdb==1.10.1

# Orchestration
dagster==1.13.6
dagster-webserver==1.13.6

# Machine Learning
scikit-learn==1.4.2
pandas==2.1.4
numpy==1.26.4
imbalanced-learn==0.14.1
joblib==1.3.2
matplotlib==3.7.5

# Experiment tracking & registry
# mlflow-skinny doit être >=2.22 pour accepter protobuf<7 (compatible avec
# dbt-core 1.11.11 et dagster 1.13.6, qui exigent tous deux protobuf<7,>=6/4)
mlflow==2.22.5

# Service ML
fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
prometheus-fastapi-instrumentator==7.0.0

# Monitoring / dérive : PSI calculé à la main (monitoring/drift_check.py),
# pas de dépendance dédiée (Evidently, etc.) pour rester dans le scope du
# module sans ajouter de dépendance lourde supplémentaire.

# Qualité / lint / tests
ruff==0.7.4
pytest==9.0.2
# httpx : requis par starlette.testclient.TestClient (utilisé par
# tests/test_api.py), non installé automatiquement avec fastapi/starlette
httpx==0.28.1
