# Données réelles (non redistribuables — licence Kaggle)
data/raw/creditcard.csv
data/raw/*.csv
!data/raw/.gitkeep
data/*.zip
data/raw/*.zip

# DuckDB
*.duckdb
*.duckdb.wal

# dbt
dbt_fraud/target/
dbt_fraud/logs/
dbt_fraud/dbt_packages/
dbt_fraud/.user.yml

# MLflow
mlruns/
mlflow.db
mlflow.db.bak-*
ml/artifacts/*.pkl
ml/artifacts/*.joblib
ml/artifacts/latest_training_run.json
!ml/artifacts/.gitkeep

# Dagster
orchestration_dagster/**/storage/
orchestration_dagster/**/history/
orchestration_dagster/**/.dagster_home/

# Monitoring runtime output
monitoring/predictions_log.jsonl
monitoring/*.html

# Continuous training platform — runtime state (per-environment, not versioned)
data/state/*.json
data/state/*.jsonl
data/state/*.lock
!data/state/.gitkeep

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Secrets / env
.env

# OS / editors
.DS_Store
.vscode/
.idea/

creds.txt
collaborators.txt